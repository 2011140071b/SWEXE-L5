<h2>ユーザー一覧</h2>

<% @users.each do |user| %>
    <p><%= user.uid %>
    <%= user.pass %>
    <%= link_to '削除', "/users/#{user.id}", method: :delete %></p>
<%end%><br>

<%= link_to 'ユーザー登録する', '/users/new' %><br>
<%= link_to '戻る', '/tweets' %>